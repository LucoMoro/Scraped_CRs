//<Beginning of snippet n. 0>
if (eventStatus.equals("important")) {
    p.setColor(importantEventColor);
} else if (eventStatus.equals("normal")) {
    p.setColor(normalEventColor);
} else {
    p.setColor(defaultColor);
}
// Removed bold text logic
/*Drawing of day number is done here
*easy to find tags draw number draw day*/
//<End of snippet n. 0>